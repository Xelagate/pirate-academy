/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findProfilePda } from "../pdas";
import { PIRATE_ACADEMY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REGISTER_PIRATE_DISCRIMINATOR = new Uint8Array([
  76, 138, 134, 64, 199, 127, 48, 152,
]);

export function getRegisterPirateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_PIRATE_DISCRIMINATOR,
  );
}

export type RegisterPirateInstruction<
  TProgram extends string = typeof PIRATE_ACADEMY_PROGRAM_ADDRESS,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterPirateInstructionData = {
  discriminator: ReadonlyUint8Array;
  crewName: string;
};

export type RegisterPirateInstructionDataArgs = { crewName: string };

export function getRegisterPirateInstructionDataEncoder(): Encoder<RegisterPirateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["crewName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_PIRATE_DISCRIMINATOR }),
  );
}

export function getRegisterPirateInstructionDataDecoder(): Decoder<RegisterPirateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["crewName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getRegisterPirateInstructionDataCodec(): Codec<
  RegisterPirateInstructionDataArgs,
  RegisterPirateInstructionData
> {
  return combineCodec(
    getRegisterPirateInstructionDataEncoder(),
    getRegisterPirateInstructionDataDecoder(),
  );
}

export type RegisterPirateAsyncInput<
  TAccountProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  profile?: Address<TAccountProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  crewName: RegisterPirateInstructionDataArgs["crewName"];
};

export async function getRegisterPirateInstructionAsync<
  TAccountProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PIRATE_ACADEMY_PROGRAM_ADDRESS,
>(
  input: RegisterPirateAsyncInput<
    TAccountProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterPirateInstruction<
    TProgramAddress,
    TAccountProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PIRATE_ACADEMY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    profile: { value: input.profile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.profile.value) {
    accounts.profile.value = await findProfilePda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.profile),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterPirateInstructionDataEncoder().encode(
      args as RegisterPirateInstructionDataArgs,
    ),
    programAddress,
  } as RegisterPirateInstruction<
    TProgramAddress,
    TAccountProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type RegisterPirateInput<
  TAccountProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  profile: Address<TAccountProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  crewName: RegisterPirateInstructionDataArgs["crewName"];
};

export function getRegisterPirateInstruction<
  TAccountProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PIRATE_ACADEMY_PROGRAM_ADDRESS,
>(
  input: RegisterPirateInput<
    TAccountProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterPirateInstruction<
  TProgramAddress,
  TAccountProfile,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PIRATE_ACADEMY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    profile: { value: input.profile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.profile),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterPirateInstructionDataEncoder().encode(
      args as RegisterPirateInstructionDataArgs,
    ),
    programAddress,
  } as RegisterPirateInstruction<
    TProgramAddress,
    TAccountProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterPirateInstruction<
  TProgram extends string = typeof PIRATE_ACADEMY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    profile: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: RegisterPirateInstructionData;
};

export function parseRegisterPirateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterPirateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      profile: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterPirateInstructionDataDecoder().decode(instruction.data),
  };
}
